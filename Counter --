if (data != null)
{
    int[] temp;
    temp = new int[data.Legth];
    
    for (int i = 0; i < data.Lenght; i++)
    {
        temp[i] = data [i];
    }
    
    data = new int[data.Lenght- 1];
    
    for (int i = 0; i < index; i++)
    {
        data [i] = temp[i];
    }
    
    for (int i = index +1; i < temp.Length; i++)
    {
        data[i - 1] = temp[i];
    }
    count--;
}
